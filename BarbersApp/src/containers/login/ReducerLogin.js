//import { actionTypes } from "./ActionsHome";

export default function loginReducer(
    state = {
        count: 0,
        name: "Alex"
    },
    action
) {
    switch (action.type) {
        default:
            return state;
    }
}
